<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >  
<sqlMap >  
  <typeAlias alias="user" type="com.express.bean.User" />  
  
  <!-- 获得全查询列表 -->  
  <select id="getAllUsers" resultClass="user">  
    select * from user  
  </select>  
    
   <!-- 根据用户名获得用户对象 -->  
   <select id="getUsersByUserName" parameterClass="String" resultClass="user">  
     select * from user where username=#username#  
  </select>   
    
  <!-- 根据id获得用户对象 -->  
    <select id="getUsersById" parameterClass="int" resultClass="user">  
     select * from user where id=#id#  
  </select>
  
</sqlMap>  